name = "libretv"
compatibility_date = "2024-01-01"
send_metrics = false

[env.production]
vars = { ENVIRONMENT = "production" }

[env.preview]
vars = { ENVIRONMENT = "preview" }

# Pages Functions 配置
# Functions 会自动从 functions/ 目录加载
# 不需要指定 main 或 assets，因为这是 Pages 项目，不是 Worker 项目